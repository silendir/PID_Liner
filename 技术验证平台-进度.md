# PID_Liner 技术验证平台 — 推进进度

> **快照日期**：2026-07-11
> **目标**：构建严谨的、100% 兼容 BetaFlight PID 系统的验证平台，为"风格化调音/反向求解"产品方向打地基
> **主线分支**：`PID_20`

---

## 🗺️ 路线图与当前位置

| 阶段 | 状态 | 关键产出 |
|---|---|---|
| 1. 源码解析 / 数学建模 | ✅ 完成 | 基于 `BF Chirp AutoTune 技术重点.md` + 现有 `BFSliderMapper` |
| 2. 前向响应系统 | ✅ 完成（粗糙）| 已被阶段 2.6 取代 |
| **2.6 前向模型映射修复** | ✅ **完成** | 控制理论推导 + 新映射器 + 11 测试全过 |
| **2.5 前向模型 RMSE 压力测试** | ✅ **完成** | 2.5a 合成闭环≈0；2.5b 真实BBL RMSE=0.067（方波单段阶跃，够支撑反解）|
| **3.0 反解方案设计（不写代码）** | ✅ **完成** | 钉死 3 选型 + 暴露机械常数标定新难点 |
| 3. 反解编码 | ⏳ 待 review | 依赖 3.0 方案确认 |
| 4. 集成测试 | ⏳ 待定 | — |

**当前位置**：3.0 方案文档出炉（`技术验证Demo/反向求解方案设计.md`），待用户 review 三选型 + 接受"机械常数标定"是新工作。

---

## ✅ 已完成：阶段 2.6 前向模型映射修复

### 修了什么

`PIDRecommendationEngine.m:117-126` 的前向模型有 4 个致命缺陷，用控制理论推导修复：

| 缺陷 | 旧（错）| 新（已验证）|
|---|---|---|
| P 影响 | `gain *= p_ratio` | `ωn *= √p_ratio` |
| D 影响 | `zeta *= √d_ratio` | `ζ *= d_ratio/√p_ratio`（耦合）|
| FF 影响 | `wn *= ff^0.3` | 独立前向通道，不进 ωn |
| I 项 | 完全丢失 | `τ_I = Kp/Ki`（补回）|

### 验证证据

11/11 单元测试通过（`BFPIDToSecondOrderMapperTests`），硬证物理方向：
- P 翻倍 → 带宽 √2 倍、ζ ÷ √2（"高 P 易震"✓）
- D 翻倍 → ζ ×2、带宽不变（"加 D 压震"✓）
- I 翻倍 → τ_I 减半（I 不再丢失）
- FF 变化 → ωn/ζ/K 全不变（独立前向）

---

## 🔑 关键技术结论（已验证，勿推翻）

1. **前向模型映射已对**：`BFPIDToSecondOrderMapper` 是正确实现，**勿复用** `PIDRecommendationEngine.m:117-126` 旧系数。
2. **反问题是病态的**（战略调研 §6.2）：4 个 PID 自由度 vs 二阶模型 3 个约束 → 反解有无穷多解，必须设计第 4 维约束。
3. **收敛 ≠ 准确**（§6.4）：`grep` 证实全项目 RMSE/残差 0 实现，predictedCurve 从未与 measuredCurve 对比 → 阶段 2.5 必须补这道质量门。
4. **推导依据**：从穿越机 rate 回路闭环特征方程 `τ_m·s² + (1+Kp·Kd)·s + Kp = 0` 解析得到 ωn/ζ。

---

## ⚠️ 陷阱与教训（避免重蹈覆辙）

| # | 陷阱 | 对策 |
|---|---|---|
| 1 | **堆粗糙代码** | `BF_反向求解验证系统.m` 等废稿被批评"没起步"。先写推导/方案文档，再写代码 |
| 2 | **三层 PID_Liner 目录** | git root → xcodeproj → app 源码。app 源码在第 3 层 `PID_Liner/PIDAnalysis/`，测试在 `PID_Liner/PID_LinerTests/` |
| 3 | **模拟器版本** | target deployment 18.5，iPhone 16 默认 18.2 会报错，用 simulatorId 指定 iOS 18.5+ 实例 |
| 4 | **RTK hook 吞 git add** | `git add` 被 hook 改写成 `rtk git add` 时静默失败。commit 用 `rtk proxy git` 绕过 |
| 5 | **Xcode 16 文件夹同步组** | `PBXFileSystemSynchronizedRootGroup`：新文件放对目录即自动入 target，无需改 pbxproj |
| 6 | **预存 pbxproj 错误** | UITests target 误引用 `IterationChain.m`（已修，从 SourcesBuildPhase 移除）|

---

## 📋 待办（按优先级）

### ✅ 阶段 2.5：前向模型 RMSE 压力测试（已完成）
- [x] **2.5a 合成数据数值闭环**：RMSE≈0，forward 模型数值正确
- [x] **2.5b 真实 BBL 测 RMSE**：RMSE=0.067（windowSize=8000 + ViewController 提纯），形状为方波单段阶跃
  - **判定**：二阶骨架够用；0.067 是 fit 方法粗糙 + 提纯残留，非阶次问题
  - 🔑 **关键澄清**：2.5b 测的是 **fit 精度**（曲线→二阶→回放），反解依赖 **forward 精度**（PID→曲线），两者不同（见 3.0 方案 §0.1）

### ✅ 阶段 3.0：反解方案设计（已完成，`技术验证Demo/反向求解方案设计.md`）
三选型已钉死：
1. **第 4 维约束** → 全曲线最小二乘 + Tikhonov 正则化（4000 点 vs 4 未知，天然过定）
2. **优化器** → Levenberg-Marquardt + 数值雅可比
3. **前向模型** → 轻量扩展（二阶 + I 稳态项 + FF 瞬态项），不换骨架

### 🔴 新发现的工程难点：机械常数标定（K_plant / τ_m）
反解走 forward 方向需**绝对** ωn=√(K_plant·P/τ_m)，必须标定机械常数（fit 用 ratio 不需要，forward 需要）。一架飞机标定一次，存飞机档案。详见 3.0 方案 §六。

### 阶段 3：反解编码（**待 review 通过后切 xhigh**）
- [ ] (可选) **2.5c 直接测 forward 精度**：扩展模型(PID)→曲线 vs 真实，确认够用再编码
- [ ] 扩展 `BFPIDToSecondOrderMapper` → 完整 `forward(PID, mechConst)`
- [ ] 实现 LM + 数值雅可比（先合成数据验证收敛性）
- [ ] 实现机械常数标定（用 `001.bbl` 已知 PID 标 K_plant/τ_m）
- [ ] 拼装反解主流程 + 双层质量门
- [ ] 真实 BBL 端到端闭环验证（已知 PID → 反解 → 能否还原）

---

## 📦 待提交文件（6 个，用户装插件中暂未提交）

```
新增:
  PID_Liner/PID_Liner/PIDAnalysis/Engine/BFPIDToSecondOrderMapper.h
  PID_Liner/PID_Liner/PIDAnalysis/Engine/BFPIDToSecondOrderMapper.m
  PID_Liner/PID_LinerTests/BFPIDToSecondOrderMapperTests.m
  技术验证Demo/前向模型真实状态审计.md
  技术验证Demo/前向模型控制理论推导.md
修改:
  PID_Liner/PID_Liner.xcodeproj/project.pbxproj  (UITests 误引用修复)
```

提交方式：`rtk proxy git` 绕过 RTK hook。

**不提交**（用户之前的修改）：`PIDCLIGenerator.m` / `PIDTuningRecord.h/m`

---

## 🗑️ 废稿处理待决（用户决定）

`技术验证Demo/` 下有 5 份过程废稿（被批评"基本乱编/没起步"），未提交：

```
技术验证Demo/后台验证Demo.m           (粗糙)
技术验证Demo/BF_PID系统严谨验证平台.m  (粗糙)
技术验证Demo/BF_反向求解验证系统.m     (粗糙,用户明确批评)
技术验证Demo/BetaFlight源码验证方案.md (粗糙)
技术验证Demo/Effort与上下文推荐值.md   (过时)
```

建议：删除（避免误导后续）。待用户确认。

---

## 🔗 相关文档

- 战略：`曲线即产品-PID_Liner差异化战略调研.md`、`风格化PID产品功能清单.md`
- 技术：`BF Chirp AutoTune 技术重点.md`、`技术验证Demo/前向模型控制理论推导.md`
- 审计：`技术验证Demo/前向模型真实状态审计.md`
- 计划：`AI进击计划.md`（与"风格化"新方向有冲突，待对齐）
