# 抖二维码传输计划 — libcimbar 光学传输 BBL（屏幕闪码 → iPhone 摄像头收）

> 创建：2026-08-21（调研完成于 2026-08-20/21，源码级查证）
> 定位：解决「电脑 → iPhone 最后一米」的 BBL 传输痛点，与 BLE 直连互补
> 调研背景：`libcimbar调研报告.md`（通用调研）+ 项目记忆 `libcimbar-bbl-channel`
> 本文档 = **可对照执行的两端（Web 发送端 + iOS 接收端）调研结论与部署计划**

---

## 一、🔑 核心结论（先读）

1. **libcimbar**（Color Icon Matrix Barcodes）：屏幕循环播放彩色图标矩阵帧，摄像头拍摄解码。实测 **~850 kbit/s ≈ 106 KB/s**，**8MB BBL ≈ 80 秒**（zstd 预压缩后更短），比 BLE（8MB 需 5-15 分钟）快 4-10 倍
2. **非对称架构定稿**：Web 永远只当**发送站**（encoder WASM），扫描器永远在 **App**（decoder native）——BBL 终点是 App 分析管线，Web 扫码器收了也进不了 App
3. **工程量已翻案**：官方已交付 encoder WASM 全套（README 明写 tested on emscripten+WASM encoder only；cimbar.org 在线 demo；releases 有产物）——原「2-4 周 WASM 移植」估算作废
4. **零协议风险 + MPL 2.0**：不碰 MSP/BLE 栈；未修改形式集成可闭源商用

## 二、三路线分工（数据采集环节，服务迭代闭环战略）

| 路线 | 场景 | 速度 | 状态 |
|---|---|---|---|
| 文件导入（UIDocumentPicker） | 通用兜底 | 手动多步 | ✅ 已有 |
| **libcimbar 光学传输（本文档）** | 🏠 有电脑调参 | **8MB≈80s** | 🟡 调研完成，待部署 |
| BLE 直连（`蓝牙对接计划.md`） | 🛩️ 飞行场无电脑（护城河） | 8MB 5-15min | 🟡 阶段0待开工 |

## 三、编码档位（B / Bm / Bu / 4C，源码 `cimb_translator/Config.h`）

| UI | modeVal | 配置 | 帧尺寸 | ECC | 场景 |
|---|---|---|---|---|---|
| **B**（默认） | 68 | Conf8x8 | 1024×1024 方 | 30/块 | 容量最大最快，产品默认 |
| Bu | 66 | Conf8x8_micro | 736×637 小 | 33/块 | 小屏幕 / 距离远 |
| Bm | 67 | Conf8x8_mini | 1024×720 宽幅 | 36/块 | 宽屏全屏 / 抗干扰最强 |
| 4C | 4 | legacy 色彩 | 1024×1024 | — | 仅兼容旧接收端，**产品 UI 隐藏** |

🔑 **收发两端必须同档**——App decoder 必须支持 B/Bm/Bu 全档位（modeVal 68/66/67），移植时不可裁剪 Config 枚举。

## 四、协议栈（发送端数据流）

```
BBL 文件
 ├─ （可选）本地加密 AES-GCM     ← 我们的自定义层（见 §六）
 ├─ zstd 压缩                    ← 库内置（level 16）
 ├─ wirehair 喷泉码               ← 前向纠错：收到足够多帧即可还原，单向无重传协商
 └─ cimbar 帧序列（彩色图标矩阵）
     ↓ 浏览器 Canvas 逐帧闪码 → 摄像头采集 → App 解码
```

- 每格 16 符号 × 颜色 = 128 种组合/格（QR 黑白 1 bit/模块 → 密度高 10 倍+）
- 单向通道：发送端零反馈，天然抗丢帧/模糊/乱序

## 五、☁️ Web 发送端（部署 + 定制）

### 5.1 官方资产（2026-08-21 实测）

| 资产 | 内容 | 体积 |
|---|---|---|
| [cimbar.org](https://cimbar.org) | 官方在线 encoder demo | — |
| `cimbar.wasm.tar.gz`（releases） | **完整站点**：sender 页 + recv 接收页 + PWA 全套 | **0.62 MB** |
| `cimbar_js.html`（releases） | 单文件版，仅 sender，下载即用零安装 | 0.99 MB |
| `cimbar.asmjs.zip` | 旧浏览器兜底 | 0.31 MB |

🔑 **「OpenCV.js 8-10MB」坑不存在**——opencv.js 只是编译期依赖，链接 tree-shake 后整个 encoder WASM 站点仅 0.62MB，首次加载秒级。

### 5.2 Cloudflare 部署能力（已查证官方 limits）

- **Pages/Worker 只当静态文件服务器**（零服务器逻辑，编码全在浏览器本地）：✅ 完全满足。0.62MB 产物 vs Pages 单文件限 25 MiB，余量 40 倍
- sender 页不需要任何设备 API（不用摄像头/蓝牙，只选文件+Canvas 渲帧）
- PWA/Service Worker 离线照常（Pages 自动 HTTPS）；免费 10 万请求/天绰绰有余
- ❌ **Worker 服务端编码不可行**：免费 CPU 10ms/次（编码是秒~分钟级）、内存 128MB 全计划硬顶、Worker 尺寸免费限 10MB——且违背气隙隐私卖点
- 部署动作：解压 tar.gz → 拖 Pages / wrangler / 连 git → 绑自有域名 CNAME，半小时级

### 5.3 定制计划（中文壳，自写不改官方文件）

**结构：官方 `cimbar_js.js` + `cimbar_js.wasm` 二进制（不动）+ 自写中文 UI 壳**

⚠️ **License 关键**：官方 `web/index.html` 是 MPL2.0 文件，直接改它 = 被修改文件必须开源。**自写全新壳**（只调用未修改的官方 WASM）→ 壳是原创代码可闭源，WASM 以未修改形式集成只需声明 MPL2.0 + 出处。

定制项：

| 项 | 做法 |
|---|---|
| 🇨🇳 全中文 UI | 自写壳：选文件 / 开始发送 / 进度 / 完成提示 |
| 🙈 隐藏 4C | 壳里不放该按钮（官方在 index.html:360） |
| 📖 档位中文说明 | B=标准（方帧·最快·默认）/ Bu=小帧（小屏幕或距离远）/ Bm=宽幅（宽屏全屏·抗干扰最强） |
| 🔐 本地加密 | 见 §六 |

工作量：**1-2 天**（纯 JS/HTML 壳 + Web Crypto，无 C++）。

### 5.4 加密后兼容性代价

加密后只有我们 App 能解（官方工具/cfc 收到是密文）——反正 decoder 本来就走 App 闭环，无损失，强化产品绑定。

### 5.5 单文件形态（2026-08-21 源码查证 `cimbar_js/CMakeLists.txt`）

- **WASM 版 = 3 文件小集合**（HTML + JS 胶水 + `cimbar_js.wasm` 外挂）——`-s WASM=1`，性能最好，**生产部署用这个**（Pages 上多文件无所谓）
- **单文件版 = asm.js 路线**（`-s SINGLE_FILE=1` 全内联）——release 的 `cimbar_js.html`(0.99MB) 就是它，本地双击即用零安装；代价是 asm.js 比 wasm 慢
- **我们的壳：UI 逻辑确实一个 HTML 写完**；WASM 引擎可选「外挂（快，上线用）」或「内嵌（便携，AirDrop 一个文件给飞手即用）」——两种形态各留一份产物，成本≈0（打包脚本差异）
- 自建 wasm 单文件（wasm base64 内嵌）：需自跑 docker 构建（emsdk `-s SINGLE_FILE=1`），🟡可选不做

### 5.6 防注入（2026-08-21 对齐：需要，但是轻量三动作）

**零后端 = 经典注入（SQL/命令）无目标**。要做的是：

| 动作 | 做法 |
|---|---|
| ① CSP 响应头 | Pages `_headers` 文件：`Content-Security-Policy: script-src 'self' 'wasm-unsafe-eval'; ...`（WASM 求值需 `wasm-unsafe-eval`） |
| ② 零第三方资源 | 不引任何 CDN/统计/字体外链 → 供应链注入面 = 0；壳里用户输入（文件名/密码）一律 `textContent` 渲染，禁 `innerHTML` |
| ③ 数据层完整性 | AES-GCM 自带认证 tag——光流被篡改 = App 解密失败，已含在 §六 加密方案内 |

可选加码：壳 fetch wasm 后 `SubtleCrypto.digest` 做 SHA-256 校验（防 wasm 文件被替换）；Cloudflare 开 HSTS。

## 六、🔐 本地加密方案（两端，已定 AES-GCM）

- **密钥**：两端手动输入同一密码 → PBKDF2 派生 → **AES-GCM**
- **发送端**（JS，Web Crypto API 原生）：文件字节 → 加密 → 喂 WASM 编码，毫秒级
- **接收端**（iOS **CryptoKit**，最低 iOS 13 正好匹配）：解码 → 解密 → 进分析管线
- 密码错 = AES-GCM 校验失败 → App 端天然有「密码错误」信号，优雅报错
- 全程零服务器零上传 = 真·本地，契合气隙气质
- 威胁模型：防旁人拍摄屏幕光流截获 BBL（飞手调参成果=机密的场景）

## 七、📱 iOS 接收端（decoder，核心投入 ~1-2 周）

### 7.1 架构

```
AVFoundation 摄像头采集（锁帧率/锁对焦/锁曝光）
  → C++ decoder（libcimbar 编进 xcframework，OpenCV 裁剪只留所需）
  → 解密（CryptoKit AES-GCM）→ .bbl 落盘
  → 喂 BlackboxDecoder（已有，零改动复用）→ 进分析管线
```

### 7.2 任务分解

| 任务 | 工作量 | 要点 |
|---|---|---|
| CMake 交叉编译 xcframework | 2-3 天 | arm64 device + simulator 双 slice；decoder 已在 android arm64 测试过（cfc） |
| OpenCV 裁剪 | 1-2 天 | **不用 CocoaPods（官方 pod 整包无子集）**，自编 OpenCV iOS：`BUILD_LIST=core,imgproc` 静态链进 xcframework（2026-08-21 源码查证：libcimbar 库路径实际只用 core+imgproc——`cvtColor/adaptiveThreshold/warpPerspective/remap/getPerspectiveTransform` 等；内嵌图标位图解析走树内 stb_image 不用 imread，`VideoCapture` 仅桌面 demo；App 喂帧走 NV12/RGB 原始像素）。App 增重 ~3-5MB（全量 pod 为 60-70MB）。解码入口=上游 `cimbar_recv_js.cpp` 原样编译（喂帧→喷泉码→zstd 还原的 extern C API，MPL 未修改合规） |
| AVFoundation 采集 | 2-3 天 | 挑 `AVCaptureDevice.Format` 锁帧率；`focusMode/exposureMode = locked`（否则码糊解码失败）；参照 sz3/cfc（Android MIT 参考实现，Camera2 参数逐项翻译） |
| CryptoKit 解密 | 0.5 天 | PBKDF2 + AES-GCM，与 §六 对应 |
| UI + 权限 | 1 天 | 首页「📷 扫码接收 BBL」入口；相机权限文案「本地解码处理，不上传任何数据」（隐私加分项） |

### 7.3 不做 Web decoder（已否决）

①官方 WASM 只测 encoder ②decoder 是 OpenCV 大户全要带 ③iOS Safari getUserMedia 帧率不稳 ④🔑 BBL 终点是 App 分析管线，Web 收了进不了 App。

（注：官方 web/ 目录有实验性 recv.html——仅备用，不影响本决策。）

## 八、⏱️ 执行阶段

| 阶段 | 内容 | 人天 | 依赖 |
|---|---|---|---|
| W1 验证 | cimbar.org / release 单文件版丢真 BBL 体验拍摄条件（距离/角度/环境光=体验下限） | 0.5 | 无 |
| W2 定制壳 | 中文壳 + 隐藏4C + 档位说明 + Web Crypto 加密 + 上 Pages 绑域名 | 1-2 | W1 体验 OK |
| R1 xcframework | decoder 交叉编译 + OpenCV 裁剪 + 单测（喂官方样本帧） | 3-5 | 无（可与 W 并行） |
| R2 App 扫码器 | AVFoundation 采集 + 解密 + 落盘 + 接分析管线 + 入口 UI | 3-5 | R1 |
| R3 联调 | 电脑闪码 ↔ iPhone 收 ↔ 进分析管线 全链路 + 密码错路径 + 三档位 | 1-2 | W2+R2 |

🔑 **R1 判据**：App decode 官方样本帧（仓库 `test/` 有 golden 帧）字节一致 = decoder 移植正确，才进 R2。
🔑 **R3 判据（整体闸门）**：真 BBL 从电脑屏幕到 App 沙盒完整落盘并可解码出 CSV 字段 = 链路全通。

## 九、⚠️ 已知坑位汇总

| # | 坑 | 对策 |
|---|---|---|
| 1 | **License**：MPL2.0 文件级弱 Copyleft | 不改库源码；自写 UI 壳；关于页声明 libcimbar + MPL2.0 |
| 2 | 相机帧率/对焦不锁定 → 解码失败 | AVFoundation 锁 Format/对焦/曝光（cfc 对照） |
| 3 | OpenCV 200MB 体积 | 自编 `BUILD_LIST=core,imgproc,imgcodecs`（不用官方 pod 整包），静态链进 xcframework，~3-5MB 落地 |
| 4 | 帧跨包重组 | 摄像头帧流按锚点定位，参照官方 decoder，勿假设固定帧率 |
| 5 | 收发档位不一致 → 无法解码 | App 支持全档位；产品默认 B |
| 6 | emsdk 版本敏感（自建 WASM 时） | 脚本 5.0.0 / 文档 3.1.39；**产品路线直接用官方 release 产物，规避此坑** |
| 7 | opencv4/ 是 submodule | 若需自建：clone 加 `--recurse-submodules` |
| 8 | iOS 后台/锁屏断摄像头 | 传输中提示保持前台 |

## 十、📁 资产与参考

- 本地 clone（临时，`/tmp/libcimbar`，2026-08-21 浅克隆）：源码结构 `src/lib/`（cimb_translator/encoder/extractor/fountain/compression/cimbar_js）、`web/`（完整站点源码）、`WASM.md`、`package-wasm.sh`。**持久化时重新 clone 到仓库技术验证Demo/ 下**
- 上游：[sz3/libcimbar](https://github.com/sz3/libcimbar)（MPL2.0）· [sz3/cfc](https://github.com/sz3/cfc)（Android 接收端参考，MIT）· [cimbar.org](https://cimbar.org)（在线 demo）· [releases](https://github.com/sz3/libcimbar/releases/latest)（cimbar.wasm.tar.gz / cimbar_js.html）
- 关联：`libcimbar调研报告.md`（通用调研全文）· `蓝牙对接计划.md`（BLE 互补路线）· 项目记忆 `libcimbar-bbl-channel` / `ble-bbl-fetch` / `iteration-closure-strategy`

---

**Why:** iOS 平台 BLE 慢 + USB 串口 MFi 限制，数据进口是用户痛点。光学传输绕开全部无线/有线协议栈，官方 encoder WASM 全套已交付使工程量降为「部署 + 壳 + App decoder」。

**执行顺序建议：** W1 体验验证先行（半天决定整个方向的手感）→ W2 部署 + R1 xcframework 并行 → R2/R3 收尾。
